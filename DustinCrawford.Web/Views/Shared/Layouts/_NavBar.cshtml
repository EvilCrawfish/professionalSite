
@{
    Layout = null;
}

<!DOCTYPE html>

<div class="brand"><a id="titleLink-header" href="http://www.dustinjcrawford.com">Dustin J Crawford</a></div>
<div class="address-bar">204 Saddleback Drive | Charlestown, IN 47111 | 502.356.2489</div>

<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li @if (ViewBag.NavPage == "Home") { @: class="active"
                                                                        }>
                    @Html.ActionLink("Home", "Home")
                </li>

                <li @if (ViewBag.NavPage == "About") { @: class="active"
                                                                        }>
                    @Html.ActionLink("About", "About")
                </li>

                <li @if (ViewBag.NavPage == "Projects") { @: class="active"
                                                                                                }>
                    @Html.ActionLink("Projects", "Projects")
                </li>

                <li @if (ViewBag.NavPage == "Contact") { @: class="active"
                                                                        }>
                    @Html.ActionLink("Contact", "Contact")
                </li>
            </ul>
        </div>
    </div>
</nav>